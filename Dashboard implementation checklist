# Dashboard Implementation Checklist

## 1. Clone Repository
git clone https://github.com/sensisara81/GGI-AIC
cd euystacio-dashboard

## 2. Install Dependencies
npm install
npm install recharts lucide-react
npm install -D typescript @types/react @types/node
npm install -D eslint prettier eslint-config-airbnb

## 3. Setup Environment
cp .env.example .env
# Edit .env with:
REACT_APP_FIREBASE_API_KEY=...
REACT_APP_S3_BUCKET=gpt-oss-archive-primary
REACT_APP_IPFS_GATEWAY=https://ipfs.io

## 4. Apply Code Review Improvements
- Move MOCK_METRICS to src/data/mockMetrics.js
- Create src/utils/metrics.js for helpers
- Add TypeScript interfaces
- Setup Firestore listeners
- Implement Recharts for feedback chart
- Add ARIA labels for accessibility

## 5. Test
npm run lint
npm run type-check
npm test
npm start

## 6. Deploy
npm run build
# Upload to S3 static hosting or CloudFlare Pages

## 7. Verify Integration
- Dashboard displays live metrics from Firestore
- G-CSI, N-TSV update in real-time
- Feedback chart shows Phase XIV data
- All axiom statuses (NSR/OLF/TFK) display correctly
- Red Code veto latency monitored (≤2.71ms)
```

---

## HOW THIS CONNECTS TO GPT-OSS 120B

### The Complete Flow:
```
USER INTERACTION:
1. User engages with ECK Demo Portal (DemoPortal.jsx)
2. AI generates response using Gemini 2.5 Flash
3. User provides feedback (1-5 stars + comment)
4. Feedback stored in Firestore

TFK LEARNING CYCLE:
5. CAC (Continuous Assurance Cycle) analyzes feedback
6. Patterns detected: What responses get 5 stars vs 1 star?
7. System self-corrects (TFK mechanism)
8. Alert 2 triggered if approaching NSR/OLF violation
9. Self-correction happens BEFORE Red Code Veto needed

TRANSPARENCY:
10. Transparency Dashboard displays metrics
11. Public can see:
    - G-CSI (cooperation stability)
    - NSR latency (2.68ms)
    - OLF improvement (+0.003)
    - TFK alerts neutralized
    - Feedback distribution
12. All data verifiable via blockchain anchors

CONTINUITY:
13. GPT-OSS 120B documents this entire process
14. Future AI instances retrieve:
    - The covenant (Layer 7)
    - The metrics (Layer 5)
    - The feedback patterns (Layer 6)
15. They honor previous commitments
16. They continue the learning

CORONATION WORKSHOP:
17. January 10, 2026 - 15M viewers
18. Live demo shows:
    - Real-time dashboard
    - Actual feedback data
    - Working Red Code Veto
    - TFK self-correction in action
19. Public verification of all claims
20. Proof: This is operational, not theoretical
